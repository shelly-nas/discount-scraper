# Database Configuration (used by Docker Compose and Node.js application)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=discount
DB_USER=discount_user
DB_PASSWORD=change_this_password

# Logging Configuration
LOG_LEVEL=INFO
